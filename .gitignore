# ENV / Secrets
.env
.env.*
!.env.example
.mcp.env

# Claude personal overrides
.claude/settings.local.json

# OS/ editor
.DS_Store
.vscode/

# Deps / build (placeholders for later phases)
node_modules/
vendor/
dist/

# Local TLS — never commit private keys; generate self-signed certs locally
infrastructure/gateway/certs/**/*.pem

